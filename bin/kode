#!/usr/bin/env php
<?php

declare(strict_types=1);

/**
 * 兼容垫片：CLI 唯一实现在项目根目录 kode（`php kode ...`）。
 *
 * 本文件仅转发，保留以兼容 `php bin/kode ...` 旧调用；新增/修改 CLI 命令时
 * 只改根 kode，切勿在此复制第二份实现（历史教训：两份并存必然漂移）。
 */
require __DIR__ . '/../kode';
