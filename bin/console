#!/usr/bin/env php
<?php

/*
 * 控制台入口
 *
 * 用法：php bin/console greet Kode --shout
 */

use Kode\Framework\Application;

require __DIR__ . '/../vendor/autoload.php';

$app = Application::make(basePath: dirname(__DIR__));

$code = $app->console()->run($argv);

exit($code);
