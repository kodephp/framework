{
    "name": "kode/orm-harness",
    "description": "独立多 ORM 压测依赖工程（不污染框架 composer.json）。由 benchmarks/peers/kode_swoole_server.php 引入其 autoloader。",
    "require": {
        "illuminate/database": "^11.0",
        "doctrine/dbal": "^4.0",
        "topthink/think-orm": "^3.0"
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
