From: benchmark investigation (kode framework vs webman/hyperf)
Subject: [kode/http] SwooleServerAdapter emit 响应取内部字符串体直发

> **状态（2026-08-23）**：本 patch 对应 spec §7.1，为 upstream-ready 版本，待反馈 kode/http 包仓库。
> 框架侧 `patches/kode-http-response-optimize.patch` 当前固化此改动（经 cweagans/composer-patches 应用）；
> 上游合入后框架侧该补丁即可删除，`composer update kode/http` 直接吃到。
> 若未来需要重建，请以 `docs/kode-http-change-spec.md` §7.1 的实现语义为准，
> 并按安装版本实际上下文重写 hunk。

背景
----
kode/http 3.4.6 起 `Response::json()` 已经 `body()` 持有内部字符串体（rawBody），
`Response::getBodyString()` 可零拷贝直接返回该字符串（`rawBody ?? (string) parent::getBody()`）。
但 `SwooleServerAdapter::run()` 的 `request` 回调中 `$swooleResponse->end()` 仍走
PSR-7 `getBody()->getContents()` 接口分发——该路径每请求多一次 `Stream` 物化
（`getBody()` 检测到 `rawBody !== null` 时 `Stream::create($rawBody)` 构造 Stream 对象，
即便 §3.6 已改为非破坏性保留 rawBody，物化本身仍有分配开销）。

直取 `getBodyString()` 可跳过该分发，零拷贝写出响应体。对非 kode 自研响应
（第三方 PSR-7 `ResponseInterface` 实现），fallback 原 `getBody()->getContents()` 路径，
保持完全向后兼容。

影响量级
-------
属卫生项（非压测差距主因，主因是 PSR-7 管线基线，由 `KODE_LEAN` 暴露）。
单请求省一次 `Stream` 对象分配 + `getContents()` 调用；在 ~150k req/s 下减少 GC 压力。
框架侧 `HttpBridge::emit` 经 `toRaw` → `getBodyString()` 已绕过此路径（toRaw ~0.3µs），
本 patch 使 kode/http 自带 `SwooleServerAdapter` 对齐同一优化。

验证
----
- `php -l src/Server/SwooleServerAdapter.php` 无语法错误。
- 冒烟：kode `Response::json()` 经 Swoole adapter emit，输出字节级与旧实现一致
  （`getBodyString()` 返回 `rawBody`，与 `getBody()->getContents()` 内容相同）。
- 非 kode PSR-7 响应（如 `Laminas\Diactoros\Response`）经 fallback 路径正常 emit。

apply 方式（在 kode/http 仓库根目录）：
    git apply patches/upstream/kode-http-swoole-emit-body.patch
或
    patch -p1 < patches/upstream/kode-http-swoole-emit-body.patch

--- a/src/Server/SwooleServerAdapter.php
+++ b/src/Server/SwooleServerAdapter.php
@@ -34,7 +34,11 @@
                 }
             }
 
-            $swooleResponse->end($response->getBody()->getContents());
+            // kode 自研响应直接取内部字符串体，避开 PSR-7 getBody()->getContents() 接口分发
+            $body = $response instanceof \Kode\Http\Response
+                ? $response->getBodyString()
+                : $response->getBody()->getContents();
+            $swooleResponse->end($body);
         });
 
         $this->server->start();
