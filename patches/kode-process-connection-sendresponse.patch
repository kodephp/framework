--- a/src/Runtime/ConnectionInterface.php	2026-08-16 11:34:37
+++ b/src/Runtime/ConnectionInterface.php	2026-08-16 11:41:28
@@ -88,6 +88,17 @@
     public function isAlive(): bool;
 
     /**
+     * 以运行时最优方式写出一个 PSR-7 响应（C 层 header()/end() 或原生对象式序列化）。
+     *
+     * 各运行时驱动内部决定具体写出策略（Swoole C 层 status()+header()+end($body)；
+     * Workerman 构造原生 Http\Response；Native 序列化为整串），业务/框架代码无需感知差异。
+     * gzip 自动压缩由运行时依据请求 Accept-Encoding 内部裁决，无需调用方传入。
+     *
+     * @param string $protocol HTTP 协议版本，默认 1.1
+     */
+    public function sendResponse(\Psr\Http\Message\ResponseInterface $response, string $protocol = '1.1'): void;
+
+    /**
      * 获取底层原生连接对象，用于访问运行时特有能力。
      *
      * @return mixed Swoole 返回 int fd；Workerman 返回 TcpConnection
--- a/src/Protocol/HttpProtocol.php	2026-08-16 11:34:37
+++ b/src/Protocol/HttpProtocol.php	2026-08-16 11:41:28
@@ -5,6 +5,7 @@
 namespace Kode\Process\Protocol;
 
 use Kode\Process\Http\Request;
+use Psr\Http\Message\ResponseInterface;
 
 /**
  * HTTP/1.1 协议实现
@@ -397,4 +398,36 @@
 
         return $resp . self::HEADER_EOF . $encoded;
     }
+
+    /**
+     * 将一个 PSR-7 响应序列化为完整 HTTP/1.1 报文（供 Native / 非 C 层运行时写出）。
+     *
+     * 状态行的协议版本取自 $protocol 参数（而非硬编码），避免下游协商出 1.0 时状态行不准确。
+     * 头名与头值经 {@see sanitizeHeaderPart()} 过滤 CR/LF/NUL，抗响应拆分。
+     * 未显式给出 Content-Length 时自动补，保证 keep-alive 下报文可正确切分。
+     */
+    public static function toRawResponse(ResponseInterface $response, string $protocol = '1.1'): string
+    {
+        $status = $response->getStatusCode();
+        $reason = $response->getReasonPhrase() ?: self::getStatusText($status);
+        $body   = (string) $response->getBody();
+
+        $out = 'HTTP/' . $protocol . ' ' . $status . ' ' . $reason . self::HEADER_EOF;
+
+        $hasContentLength = false;
+        foreach ($response->getHeaders() as $name => $values) {
+            if (strtolower((string) $name) === 'content-length') {
+                $hasContentLength = true;
+            }
+            foreach ($values as $value) {
+                $out .= self::headerLine((string) $name, (string) $value);
+            }
+        }
+
+        if (!$hasContentLength) {
+            $out .= 'Content-Length: ' . strlen($body) . self::HEADER_EOF;
+        }
+
+        return $out . self::EOF . $body;
+    }
 }
--- a/src/Runtime/Driver/SwooleConnection.php	2026-08-16 11:34:37
+++ b/src/Runtime/Driver/SwooleConnection.php	2026-08-16 11:41:28
@@ -6,6 +6,7 @@
 
 use Kode\Process\Protocol\HttpProtocol;
 use Kode\Process\Runtime\ConnectionInterface;
+use Psr\Http\Message\ResponseInterface;
 
 /**
  * Swoole 连接适配。
@@ -105,6 +106,43 @@
 
         // 非 HTTP 模式（裸 TCP / WebSocket）：降级为普通发送
         return $this->send($data);
+    }
+
+    public function sendResponse(ResponseInterface $response, string $protocol = '1.1'): void
+    {
+        if ($this->response === null) {
+            // 非 HTTP 模式：降级为裸报文发送
+            $this->send(HttpProtocol::toRawResponse($response, $protocol), true);
+            return;
+        }
+        if ($this->responded) {
+            return;
+        }
+        // 自动 gzip：请求接受压缩且响应体达阈值时，退回整串压缩发送
+        if ($this->gzipAuto) {
+            $this->send(HttpProtocol::toRawResponse($response, $protocol), true);
+            return;
+        }
+
+        $status = $response->getStatusCode();
+        $this->response->status($status, $response->getReasonPhrase() ?: HttpProtocol::getStatusText($status));
+
+        $body = (string) $response->getBody();
+        $hasContentLength = false;
+        foreach ($response->getHeaders() as $name => $values) {
+            if (strtolower((string) $name) === 'content-length') {
+                $hasContentLength = true;
+            }
+            foreach ($values as $value) {
+                $this->response->header((string) $name, (string) $value);
+            }
+        }
+        if (!$hasContentLength) {
+            $this->response->header('Content-Length', (string) strlen($body));
+        }
+
+        $this->responded = true;
+        $this->response->end($body);
     }
 
     public function isChunkStarted(): bool
--- a/src/Runtime/Driver/WorkermanConnection.php	2026-08-16 11:34:37
+++ b/src/Runtime/Driver/WorkermanConnection.php	2026-08-16 11:41:28
@@ -6,6 +6,7 @@
 
 use Kode\Process\Protocol\HttpProtocol;
 use Kode\Process\Runtime\ConnectionInterface;
+use Psr\Http\Message\ResponseInterface;
 
 /**
  * Workerman TcpConnection 的统一连接适配。
@@ -70,6 +71,23 @@
         return (bool)$this->conn->send($compressed, true);
     }
 
+    public function sendResponse(ResponseInterface $response, string $protocol = '1.1'): void
+    {
+        // 自动 gzip：降级为整串压缩发送（WorkermanConnection::send 内部完成压缩）
+        if ($this->gzipAuto) {
+            $this->send(HttpProtocol::toRawResponse($response, $protocol), true);
+            return;
+        }
+
+        $body = (string) $response->getBody();
+        $wr = new \Workerman\Protocols\Http\Response(
+            $response->getStatusCode(),
+            $response->getHeaders(),
+            $body,
+        );
+        $this->conn->send($wr);
+    }
+
     public function isChunkStarted(): bool
     {
         return $this->chunkStarted;
--- a/src/Runtime/Driver/NativeConnection.php	2026-08-16 11:34:37
+++ b/src/Runtime/Driver/NativeConnection.php	2026-08-16 11:41:28
@@ -9,6 +9,7 @@
 use Kode\Process\Protocol\HttpProtocol;
 use Kode\Process\Reactor\LoopInterface;
 use Kode\Process\Runtime\ConnectionInterface;
+use Psr\Http\Message\ResponseInterface;
 
 /**
  * 自研（Native）运行时的连接对象。
@@ -385,6 +386,12 @@
         $this->closeAfterFlush = true;
     }
 
+    public function sendResponse(ResponseInterface $response, string $protocol = '1.1'): void
+    {
+        // Native 运行时：序列化为完整 HTTP 报文，raw 写出（压缩由运行时内部 gzipAuto 路径处理）
+        $this->send(HttpProtocol::toRawResponse($response, $protocol), true);
+    }
+
     public function close(?string $data = null): void
     {
         if ($this->closed) {
--- a/src/Runtime/Driver/Http2Stream.php	2026-08-16 11:34:37
+++ b/src/Runtime/Driver/Http2Stream.php	2026-08-16 11:41:28
@@ -7,6 +7,7 @@
 use Kode\Process\Protocol\Http2\Http2Session;
 use Kode\Process\Protocol\HttpProtocol;
 use Kode\Process\Runtime\ConnectionInterface;
+use Psr\Http\Message\ResponseInterface;
 
 /**
  * HTTP/2 单条流的连接视图。
@@ -205,6 +206,15 @@
         $this->session->respond($this->streamId, $status, $normalized, $data);
 
         return $this->parent->flushHttp2();
+    }
+
+    public function sendResponse(ResponseInterface $response, string $protocol = '1.1'): void
+    {
+        if ($this->closed || $this->responded) {
+            return;
+        }
+        // HTTP/2 同样经 send() 规整：完整 HTTP 报文 → 映射为 HEADERS + DATA
+        $this->send(HttpProtocol::toRawResponse($response, $protocol), true);
     }
 
     public function setGzipAuto(bool $enabled): void
--- a/tests/Cluster/RpcServerTest.php	2026-08-16 11:34:37
+++ b/tests/Cluster/RpcServerTest.php	2026-08-16 11:41:28
@@ -7,6 +7,7 @@
 use Kode\Process\Cluster\Rpc\RpcFrame;
 use Kode\Process\Cluster\Rpc\RpcServer;
 use Kode\Process\Runtime\ConnectionInterface;
+use Psr\Http\Message\ResponseInterface;
 use PHPUnit\Framework\TestCase;
 use ReflectionMethod;
 use RuntimeException;
@@ -261,6 +262,11 @@
     public function getContext(string $key, mixed $default = null): mixed
     {
         return $default;
+    }
+
+    public function sendResponse(ResponseInterface $response, string $protocol = '1.1'): void
+    {
+        $this->sent[] = (string) $response->getBody();
     }
 
     public function chunk(string $data): bool
